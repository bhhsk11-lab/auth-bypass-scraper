# ── Web framework ──────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Stealth HTTP (TLS impersonation) ───────────────────────────
curl_cffi==0.7.3

# ── Async HTTP (FlareSolverr / ScraperAPI fallbacks) ───────────
httpx[http2]==0.27.2

# ── Stealth browser ────────────────────────────────────────────
playwright==1.47.0
# NOTE: browser binaries are installed at build time via the Dockerfile's
#   RUN playwright install chromium
# (deliberately WITHOUT --with-deps: that flag makes Playwright resolve
# its own apt package list, which references font package names that no
# longer exist on Debian "trixie" (e.g. ttf-unifont/ttf-ubuntu-font-family)
# and fails the build. The Dockerfile installs the equivalent libraries
# itself by exact package name instead, then this line just fetches the
# browser binary.)

# ── HTML parsing / extraction ──────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.3.0
readability-lxml==0.8.1

# ── PDF processing ─────────────────────────────────────────────
pypdf==5.0.1
pdfplumber==0.11.4
pdf2image==1.17.0
# System deps needed by pdf2image: poppler-utils (in Dockerfile apt-get)

# ── OCR ────────────────────────────────────────────────────────
pytesseract==0.3.13
Pillow==10.4.0
# System deps needed: tesseract-ocr (in Dockerfile apt-get)

# ── Math humanization ──────────────────────────────────────────
sympy==1.13.3
